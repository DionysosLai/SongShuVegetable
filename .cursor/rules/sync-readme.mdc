---
description: 当项目配置、规则、目录结构或 MCP 有自定义变更时，同步更新 README.md
alwaysApply: true
---

# README 同步规范

每当对以下内容做出**自定义修改**时，必须同步更新 `README.md` 中对应的说明：

## 需要同步的变更类型

| 变更内容 | README 中需更新的章节 |
|---|---|
| `.cursor/rules/` 新增或修改规则 | 新增"Cursor Rules 说明"章节或更新对应条目 |
| `.cursor/mcp.json` 配置变更（新增/删除 MCP、参数变化） | "Cursor MCP 配置"章节 |
| `Assets/` 目录结构调整 | "项目目录结构"章节 |
| Unity 版本升级 | "环境要求"章节 |
| 新增第三方插件/依赖 | "环境要求"或新增"依赖说明"章节 |

## 同步原则

- README 的描述要与实际配置**保持一致**，不能有过时信息
- 如果某项配置被删除，README 中对应说明也要删除
- MCP 配置模板中的路径占位符格式统一用 `<说明文字>` 表示
